'use client';

export default function CapsulesDisabledPage() {
  return (
    <main className="min-h-[60vh] flex items-center justify-center">
      <p className="text-sm text-muted-foreground">
        La fonctionnalité "capsules" a été désactivée. Utilise le plan pédagogique (onglet Séances) et l'onglet Programme dans la page de stage.
      </p>
    </main>
  );
}
